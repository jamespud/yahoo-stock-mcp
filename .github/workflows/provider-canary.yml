name: Live provider canary

on:
  schedule:
    - cron: "17 1,13 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: live-provider-canary
  cancel-in-progress: false

jobs:
  providers:
    name: Live provider contracts
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # Yahoo contract failures hard-fail. Investing network/access availability can be DEGRADED
      # because hosted-runner egress may be blocked; once Investing returns a snapshot, its
      # schema/unit contract assertions hard-fail on regression.
      - run: npm run canary:providers
